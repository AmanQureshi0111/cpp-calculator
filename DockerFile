FROM gcc:latest

# Install dependencies
RUN apt-get update && apt-get install -y cmake git

WORKDIR /app
COPY . .

# Compile program
RUN g++ main.cpp -o calculator -pthread

# Expose port
EXPOSE 8080

# Run program
CMD ["./calculator"]
